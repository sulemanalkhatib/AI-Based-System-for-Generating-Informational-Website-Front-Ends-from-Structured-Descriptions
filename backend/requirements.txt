fastapi>=0.115
uvicorn[standard]>=0.30
sse-starlette>=2.1
openai>=1.50
aiosqlite>=0.20
pydantic>=2.9
python-dotenv>=1.0
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27

# Optional — only needed if you enable the Visual audit in Settings:
#   pip install playwright && playwright install chromium
# playwright>=1.47
